---
title: 属性、费用、冷却、伤害与死亡
description: 沿 HealthSet 和 HealthComponent 区分数值修改、属性通知及死亡状态推进。
---

## 学习目标与前置条件

前置：AbilitySet 与技能激活。目标是能解释伤害从 Effect 到血条、死亡和重生的路径。源码核对于 2026-09-13，UE 5.8.1。

AttributeSet 定义属性，GameplayEffect 描述修改规则，EffectSpec 承载一次应用的等级、上下文和动态数值。技能负责何时尝试并提交操作；UI 观察结果，不决定权威生命值。

## 操作步骤

1. 打开 `/Game/GameplayEffects/Damage/GE_Damage_Basic_SetByCaller` 与 `/Game/GameplayEffects/Heal/GE_Heal_Instant`，观察修饰目标、持续策略和数值来源。
2. 打开 `/Game/GameplayEffects/GE_HeroDash_Cooldown`，区分持续时间和标记冷却状态的标签。不同引擎版本可能用不同组件界面表达 GE 标签，按本机 Details 为准。
3. 在 `ULyraHealthSet::PostGameplayEffectExecute` 设置断点，观察执行属性是 Damage、Healing 还是 Health。
4. 跟进 `ULyraHealthComponent::HandleOutOfHealth`，查看死亡 GameplayEvent 的目标 ASC。
5. 在 `/Game/Characters/Heroes/Abilities/GA_Hero_Death` 中检查父类和图，沿 StartDeath、FinishDeath 到角色销毁/重生的执行过程。

## Damage 和 Health 不一样

`Source/LyraGame/AbilitySystem/Attributes/LyraHealthSet.cpp` 会消耗 Damage 元属性并清零：

```cpp
SetHealth(FMath::Clamp(GetHealth() - GetDamage(), MinimumHealth, GetMaxHealth()));
SetDamage(0.0f);
```

因此 Damage 不是“剩余生命”。Healing 同样用于一次治疗计算后归零。Health 在合法范围内被钳制；免伤和开发者 GodMode 等条件也会影响结果。只盯着最终 Health 数字，可能漏掉伤害在上游被拒绝的原因。

HealthSet 发送属性变化和耗尽事件，HealthComponent 提供更方便的角色侧通知，并复制死亡状态。`OnRep_DeathState` 处理从未死亡直接收到死亡完成的情况，补走 StartDeath 与 FinishDeath，说明客户端不一定观察到每一个服务器中间状态。

## 费用与冷却什么时候扣

先检查能否激活，再在适当时间 `CommitAbility`，避免技能开始播放却无条件扣两次费用。Lyra 的 `ULyraAbilityCost` 支持额外费用逻辑；装备弹药和普通 GAS Attribute 费用的存储位置可能不同。

SetByCaller 需要在 EffectSpec 上设置与资产一致的标签和值；只创建 GE 资产不会自动知道“这次伤害是 20”。对照调用方构造 Spec 的代码和蓝图引脚，而不是只编辑 GE 默认值。

{/* full-guide-begin */}

## 看懂一次真实步枪伤害配置和计算

![步枪伤害效果：执行类 LyraDamageExecution 与 BaseDamage 的 12.0 配置](/media/beginner/whole-guide/rifle-damage-effect.png)

**看图操作**：打开 /ShooterCore/Weapons/Rifle/GE_Damage_RifleAuto 的类默认值，展开 执行 → 索引[0] → 计算修改器 → 索引[0]。图中捕获 Source 的 BaseDamage，操作 Add (Base)，幅度类型 Scalable Float，常量 12.0；下方持续策略是 Instant。

注意外层“修饰符”为 0，并不代表没有伤害：本资产配置的是 **Execution（执行计算）**，其内部计算修改器提供 BaseDamage。12 是这里的配置值，最终扣血仍经过队伍允许、距离和物理材质衰减等条件。
源码：`Source/LyraGame/AbilitySystem/Executions/LyraDamageExecution.cpp`，本机第 131–137 行。下面是实际节选；在 Rider 中按文件名和函数名定位，完整上下文以源文件为准。

```cpp
const float DamageDone = FMath::Max(BaseDamage * DistanceAttenuation * PhysicalMaterialAttenuation * DamageInteractionAllowedMultiplier, 0.0f);

if (DamageDone > 0.0f)
{
	// Apply a damage modifier, this gets turned into - health on the target
	OutExecutionOutput.AddOutputModifier(FGameplayModifierEvaluatedData(ULyraHealthSet::GetDamageAttribute(), EGameplayModOp::Additive, DamageDone));
}
```
![从伤害配置到生命变化和死亡通知](/media/guide-diagrams/04-gas-effects-health.svg)

**读图**：例如无衰减且允许伤害时，12 可参与一次 100→88 的扣血计算；这是条件算例，不是保证任何距离或目标都固定扣 12。

### 分开三种时间含义

| 类型 | 你要读什么 | 不能直接推断什么 |
|---|---|---|
| Instant | 一次应用时执行的数值修改 | 不代表没有复制通知或后续死亡事件 |
| 有持续时间的效果 | Duration、Period、相关标签/组件 | 有 Duration 不表示每帧都重复扣数值 |
| 冷却 | Commit、冷却效果、持续时间和冷却标签 | 仅创建一个名叫 Cooldown 的资产不会阻止技能 |

**验证练习**：先用一类目标、一个固定位置、一次开火观察 `DamageDone` 与 Health 前后值。若没有输出，先检查上游乘数和目标条件；若生命改变但 UI 不变，再查通知/绑定。不要在 UI 的 Tick 中手动减 12 来模仿服务器状态。

{/* full-guide-end */}

## 预期结果、排障与练习

应能观察一次应用只扣一次数值、冷却期间拒绝重新激活、生命耗尽只启动一次死亡流程。重生后 UI 仍显示旧数值时，查属性委托与 Avatar 更换；连续触发死亡则查耗尽边界与状态守卫。

练习：分别画出持续治疗、瞬间伤害和冷却的时间线。指出哪个数值需要复制、哪个只是执行时的元属性，以及独立服务器断开重启后为什么仍需要数据库层恢复角色进度。

{/* copy-course-exercise */}

## 按步骤完成一次改造

实验四将 Rifle 的 BaseDamage 从 12 改为 8，并在 Execution 与 HealthSet 两处观察数值，区分基础量和实际扣血。 [打开六个改造实验](/docs/02-architecture/modification-workshop)。
