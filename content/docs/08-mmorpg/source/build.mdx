---
title: 工程入口与构建目标：完整文件
description: 按目标路径新建或替换文件，复制完整实现，再与逐步操作课程对照。
---

先复制 00 骨架，再比较 03 的完整依赖。Target 决定构建 Editor、Client、Server 或 Program；Build.cs 决定某一个模块能引用哪些模块。

[返回对应操作与解释](/docs/08-mmorpg/from-zero) · [返回制作主线](/docs/08-mmorpg/from-zero)

![完整文件怎样变成可以运行的游戏](/media/guide-diagrams/08-mmorpg-source-workbook.svg)

## 复制之前先确认位置

下列路径均相对你的 **MMORPG 工程根目录**。在 Rider 的文件视图中右键父目录 → New → File，输入完整文件名（包括扩展名）；文件已存在时替换整个文件，不能把第二套类声明追加到末尾。页面给出的是当前教程的完整文件，代码框右上角可以复制，不含省略号占位实现。

先写 `.h` 再写 `.cpp` 便于阅读；**完成当前源码阶段的全部文件后才编译**。`*.generated.h` 由 UnrealHeaderTool 生成，不手工创建。`#include`、模块导出宏和函数签名都要保留。一个文件中出现多个类时，复制整个文件一次即可。

按 [四个源码阶段](/docs/08-mmorpg/from-zero) 使用这些文件。本组从 **03 阶段**开始使用。00–02 阶段保留当阶段的轻量 `.uproject` 和 Build.cs；03 阶段再切换到完整项目依赖。

## 逐段理解与资产连接

### 代码、插件和构建目标的关系

`.uproject.Modules` 只列游戏运行模块，不需要把所有依赖挨个写进这个数组；依赖来自各 Build.cs。`ExtraModuleNames.Add("MMORPG")` 把游戏模块加入目标。`MMORPG_API` 是模块导出宏，跨模块引用时需要保留。

MMOBackendService 是 Program，自己的 Target 指定 LaunchModuleName；它不是在游戏内 Spawn 的 Actor。先做阶段 00 的轻量版本，再到 03 替换完整 Build.cs 和描述符。

## 本组文件清单

| 文件 | 行数 | 操作 |
|---|---:|---|
| `MMORPG.uproject` | 25 | 新建；已有同名文件则整体替换 |
| `Source/MMORPG.Target.cs` | 11 | 新建；已有同名文件则整体替换 |
| `Source/MMORPG/MMORPG.Build.cs` | 12 | 新建；已有同名文件则整体替换 |
| `Source/MMORPG/MMORPG.cpp` | 2 | 新建；已有同名文件则整体替换 |
| `Source/MMORPGClient.Target.cs` | 11 | 新建；已有同名文件则整体替换 |
| `Source/MMORPGEditor.Target.cs` | 11 | 新建；已有同名文件则整体替换 |
| `Source/MMORPGServer.Target.cs` | 11 | 新建；已有同名文件则整体替换 |

## 文件 1：MMORPG.uproject

目标路径：`MMORPG.uproject`。原文件位于包内 `examples/MMORPG/MMORPG.uproject`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```json title="MMORPG.uproject"
{
  "FileVersion": 3,
  "EngineAssociation": "",
  "Category": "Education",
  "Description": "Lyra patterns rebuilt as a small authenticated multiplayer RPG.",
  "Modules": [{"Name":"MMORPG","Type":"Runtime","LoadingPhase":"Default"}],
  "Plugins": [
    {"Name":"GameplayAbilities","Enabled":true},
    {"Name":"EnhancedInput","Enabled":true},
    {"Name":"ModularGameplay","Enabled":true},
    {"Name":"ModularGameplayActors","Enabled":true},
    {"Name":"GameFeatures","Enabled":true},
    {"Name":"CommonUI","Enabled":true},
    {"Name":"CommonGame","Enabled":true},
    {"Name":"CommonUser","Enabled":true},
    {"Name":"UIExtension","Enabled":true},
    {"Name":"ModelViewViewModel","Enabled":true},
    {"Name":"MMOIntegration","Enabled":true},
    {"Name":"MMOFramework","Enabled":true},
    {"Name":"MMOCore","Enabled":true},
    {"Name":"MMODocTools","Enabled":true,"TargetAllowList":["Editor"]},
    {"Name":"PythonScriptPlugin","Enabled":true,"TargetAllowList":["Editor"]},
    {"Name":"EditorScriptingUtilities","Enabled":true,"TargetAllowList":["Editor"]}
  ]
}
```

</details>

## 文件 2：MMORPG.Target.cs

目标路径：`Source/MMORPG.Target.cs`。原文件位于包内 `examples/MMORPG/Source/MMORPG.Target.cs`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```csharp title="Source/MMORPG.Target.cs"
using UnrealBuildTool;
public class MMORPGTarget : TargetRules
{
    public MMORPGTarget(TargetInfo Target) : base(Target)
    {
        Type = TargetType.Game;
        DefaultBuildSettings = BuildSettingsVersion.V7;
        IncludeOrderVersion = EngineIncludeOrderVersion.Latest;
        ExtraModuleNames.Add("MMORPG");
    }
}
```

</details>

## 文件 3：MMORPG.Build.cs

目标路径：`Source/MMORPG/MMORPG.Build.cs`。原文件位于包内 `examples/MMORPG/Source/MMORPG/MMORPG.Build.cs`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```csharp title="Source/MMORPG/MMORPG.Build.cs"
using UnrealBuildTool;
public class MMORPG : ModuleRules
{
    public MMORPG(ReadOnlyTargetRules Target) : base(Target)
    {
        PCHUsage = PCHUsageMode.UseExplicitOrSharedPCHs;
        PublicDependencyModuleNames.AddRange(new[] { "Core", "CoreUObject", "Engine", "InputCore", "EnhancedInput", "GameplayAbilities", "GameplayTags", "GameplayTasks", "ModularGameplay", "ModularGameplayActors", "GameFeatures", "CommonUI", "CommonInput", "CommonGame", "UIExtension", "UMG", "Slate", "SlateCore", "ModelViewViewModel", "FieldNotification", "Json", "HTTP", "MMOBackendClient" });
        if (Target.Type != TargetType.Client) PrivateDependencyModuleNames.Add("MMOBackendServer");
        PublicDependencyModuleNames.Add("MMOFramework");
        PrivateDependencyModuleNames.Add("AnimGraphRuntime");
    }
}
```

</details>

## 文件 4：MMORPG.cpp

目标路径：`Source/MMORPG/MMORPG.cpp`。原文件位于包内 `examples/MMORPG/Source/MMORPG/MMORPG.cpp`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```cpp title="Source/MMORPG/MMORPG.cpp"
#include "Modules/ModuleManager.h"
IMPLEMENT_PRIMARY_GAME_MODULE(FDefaultGameModuleImpl, MMORPG, "MMORPG");
```

</details>

## 文件 5：MMORPGClient.Target.cs

目标路径：`Source/MMORPGClient.Target.cs`。原文件位于包内 `examples/MMORPG/Source/MMORPGClient.Target.cs`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```csharp title="Source/MMORPGClient.Target.cs"
using UnrealBuildTool;
public class MMORPGClientTarget : TargetRules
{
    public MMORPGClientTarget(TargetInfo Target) : base(Target)
    {
        Type = TargetType.Client;
        DefaultBuildSettings = BuildSettingsVersion.V7;
        IncludeOrderVersion = EngineIncludeOrderVersion.Latest;
        ExtraModuleNames.Add("MMORPG");
    }
}
```

</details>

## 文件 6：MMORPGEditor.Target.cs

目标路径：`Source/MMORPGEditor.Target.cs`。原文件位于包内 `examples/MMORPG/Source/MMORPGEditor.Target.cs`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```csharp title="Source/MMORPGEditor.Target.cs"
using UnrealBuildTool;
public class MMORPGEditorTarget : TargetRules
{
    public MMORPGEditorTarget(TargetInfo Target) : base(Target)
    {
        Type = TargetType.Editor;
        DefaultBuildSettings = BuildSettingsVersion.V7;
        IncludeOrderVersion = EngineIncludeOrderVersion.Latest;
        ExtraModuleNames.Add("MMORPG");
    }
}
```

</details>

## 文件 7：MMORPGServer.Target.cs

目标路径：`Source/MMORPGServer.Target.cs`。原文件位于包内 `examples/MMORPG/Source/MMORPGServer.Target.cs`，不是需要修改的引擎文件。

<details>
<summary>展开完整文件并复制</summary>

```csharp title="Source/MMORPGServer.Target.cs"
using UnrealBuildTool;
public class MMORPGServerTarget : TargetRules
{
    public MMORPGServerTarget(TargetInfo Target) : base(Target)
    {
        Type = TargetType.Server;
        DefaultBuildSettings = BuildSettingsVersion.V7;
        IncludeOrderVersion = EngineIncludeOrderVersion.Latest;
        ExtraModuleNames.Add("MMORPG");
    }
}
```

</details>

## 写完后怎样确认

保存全部文件，回到当前阶段执行文件核对和 Editor 构建。文件核对只能发现缺文件、复制不完整和版本差异；`Result: Succeeded` 才说明编译成功。资产、UI 与联机行为继续按 [对应课程](/docs/08-mmorpg/from-zero) 验收。
